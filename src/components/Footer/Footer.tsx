import classes from './Footer.module.css';

const Footer = () => {
    return (
        <div className={classes.footer}>
            <p className={classes.copyright}>Todos os direitos reservados</p>
        </div>
    )
};

export default Footer;